import React from 'react';

import DetailsView from './Details.view';

const Details = (): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <DetailsView {...generatedProps} />;
};

export default Details;
