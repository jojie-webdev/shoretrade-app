import React from 'react';

// import { useTheme } from 'utils/Theme';
import { CreateAssistantGeneratedProps } from './CreateAssistant.props';
import { Container } from './CreateAssistant.style';

const CreateAssistantView = (props: CreateAssistantGeneratedProps) => {
  // const theme = useTheme();
  return (
    <Container>
      <h1>CreateAssistant Screen</h1>
    </Container>
  );
};

export default CreateAssistantView;