import React from 'react';

// import { useTheme } from 'utils/Theme';
import { MarketPricesGeneratedProps } from './MarketPrices.props';
import { Container } from './MarketPrices.style';

const MarketPricesView = (props: MarketPricesGeneratedProps) => {
// const theme = useTheme();
return (
<Container>
  <h1>MarketPrices Screen</h1>
</Container>
);
};

export default MarketPricesView;