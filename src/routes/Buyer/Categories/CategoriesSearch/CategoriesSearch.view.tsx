import React from 'react';

// import { useTheme } from 'utils/Theme';
import { CategoriesSearchGeneratedProps } from './CategoriesSearch.props';
import { Container } from './CategoriesSearch.style';

const CategoriesSearchView = (props: CategoriesSearchGeneratedProps) => {
  // const theme = useTheme();
  return (
    <Container>
      <h1>CategoriesSearch Screen</h1>
    </Container>
  );
};

export default CategoriesSearchView;