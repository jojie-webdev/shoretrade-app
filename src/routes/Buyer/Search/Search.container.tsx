import React from 'react';

import SearchView from './Search.view';

const Search = (): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <SearchView {...generatedProps} />;
};

export default Search;
