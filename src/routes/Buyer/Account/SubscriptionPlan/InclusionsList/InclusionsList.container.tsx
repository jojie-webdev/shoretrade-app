import React from 'react';

import InclusionsListView from './InclusionsList.view';

const InclusionsList = (): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <InclusionsListView {...generatedProps} />;
};

export default InclusionsList;
