import React from 'react';

// import { useTheme } from 'utils/Theme';
import { AddAssistantGeneratedProps } from './AddAssistant.props';
import { Container } from './AddAssistant.style';

const AddAssistantView = (props: AddAssistantGeneratedProps) => {
  // const theme = useTheme();
  return (
    <Container>
      <h1>AddAssistant Screen</h1>
    </Container>
  );
};

export default AddAssistantView;
