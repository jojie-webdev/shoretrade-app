import { Action } from 'types/Action';
import { {{pascalCase name}}State } from 'types/store/{{pascalCase name}}State';
import pathOr from 'ramda/es/pathOr';
import { {{camelCase name}}Actions } from '../actions';

const DEFAULT_STATE: {{pascalCase name}}State = {
  // TODO: Add store values
};

const DEFAULT_ACTION: Action<{{pascalCase name}}State> = {
  type: '',
  payload: {
    // TODO: Add store values
  },
};

export default (state = DEFAULT_STATE, action = DEFAULT_ACTION) => {
  return pathOr(state, [action.type], {
    [{{camelCase name}}Actions.UPDATE]: {
      ...state,
      ...action.payload,
    },
    [{{camelCase name}}Actions.CLEAR]: {},
  });
};
